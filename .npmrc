# Make the `engines` floor in package.json an ERROR rather than a warning.
# Astro refuses to build below Node 22.12, and without this a wrong local
# version installs happily and only fails at build time — or worse, builds
# locally and fails in CI, which is how the first publish broke.
engine-strict=true
